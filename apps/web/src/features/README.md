# Features

As features agrupam componentes, estilos, tipos e configuração por domínio.
Atualmente existe apenas [`events/`](events/README.md), que implementa o
workspace principal do backoffice.

Uma feature pode consumir componentes comuns e services, mas deve manter no
próprio diretório os tipos e o estado específicos do domínio.

